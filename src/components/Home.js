import React from 'react';
// import { connect } from 'react-redux';

const Home = () => (
	<div>
	Home is from server this
	</div>
);

/**
 * Connect to Redux store.
 */
export default Home;
